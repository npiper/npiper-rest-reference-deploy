apiVersion: v1
kind: Service
metadata:
  name: {{ template "npiper-rest-reference.fullname" . }}
  labels:
    app: {{ template "npiper-rest-reference.name" . }}
    chart: {{ template "npiper-rest-reference.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "npiper-rest-reference.name" . }}
    release: {{ .Release.Name }}
